# Імпорт даних на Render

## ⚠️ Важливо про дані

**Дані НЕ підвантажуються автоматично!** Після деплою потрібно:
1. Виконати міграції (вже в start.sh)
2. Імпортувати статті вручну

## 📋 Як імпортувати дані

### Варіант 1: Через Shell в Render Dashboard (Рекомендовано)

1. Відкрийте **Shell** в Render Dashboard
2. Виконайте команди:

```bash
# Створити суперкористувача (якщо потрібно)
python manage.py createsuperuser

# Імпортувати статті
python manage.py import_articles

# Або створити тестові дані
python manage.py create_sample_data
```

### Варіант 2: Додати автоматичний імпорт (опціонально)

Можна додати імпорт в `start.sh`, але це буде виконуватися при кожному запуску (не рекомендовано для production).

## 🔄 Якщо використовуєте SQLite

**Увага:** Дані будуть втрачатися при кожному деплої!

Після кожного деплою потрібно повторно імпортувати дані через Shell.

## ✅ Якщо використовуєте PostgreSQL

Дані зберігаються між деплоями. Потрібно імпортувати тільки один раз після першого деплою.

## 📝 Команди для імпорту

### Імпорт статей з JSON (якщо є файл)
```bash
python manage.py import_articles
```

### Створення тестових даних
```bash
python manage.py create_sample_data
```

### Створення суперкористувача
```bash
python manage.py createsuperuser
```

## 💡 Рекомендація

1. **Створіть PostgreSQL** на Render (безкоштовно)
2. Після першого деплою виконайте через Shell:
   ```bash
   python manage.py migrate
   python manage.py createsuperuser
   python manage.py import_articles
   ```
3. Дані зберігатимуться між деплоями

